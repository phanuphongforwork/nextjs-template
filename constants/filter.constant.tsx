export const FilterConstant = {
  type: {
    select: "select",
    date: "date",
    checkbox: "checkbox",
  },
};

export enum FilterType {
  select = "select",
  date = "date",
  checkbox = "checkbox",
}
